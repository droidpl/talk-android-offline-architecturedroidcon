<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobgen.droidcon.offline">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DemosApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".shared.ui.DemoSelectorActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".demos.online.OnlinePostActivity" />

        <activity android:name=".demos.online.OnlinePostDetailsActivity" />

        <activity android:name=".demos.offline.OfflinePostActivity"/>

        <activity android:name=".demos.offline.OfflinePostDetailsActivity"/>
    </application>

</manifest>